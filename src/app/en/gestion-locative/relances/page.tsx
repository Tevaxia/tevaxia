import { editorialPageMetadata } from "@/lib/editorial-seo";
export { default } from "@/app/gestion-locative/relances/page";

export const generateMetadata = () => editorialPageMetadata("/gestion-locative/relances");

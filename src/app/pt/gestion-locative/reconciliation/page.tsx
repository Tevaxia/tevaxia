import { editorialPageMetadata } from "@/lib/editorial-seo";
export { default } from "@/app/gestion-locative/reconciliation/page";

export const generateMetadata = () => editorialPageMetadata("/gestion-locative/reconciliation");
